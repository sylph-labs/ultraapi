[package]
name = "sqlx-example"
version = "0.1.0"
edition = "2021"

[dependencies]
ultraapi = { path = "../../ultraapi" }
sqlx = { version = "0.8", features = ["runtime-tokio", "sqlite"] }
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
