[package]
name = "grpc-example"
version = "0.1.0"
edition = "2021"

[dependencies]
ultraapi = { path = "../../ultraapi" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
